

export const ROTATION_ANGLE = Math.PI / 2;
export const BASE_SPEED = 100;
export const MAX_RUBBER = 10;
// You can also use Enums for grouped discrete values
export enum Direction {
    UP = -Math.PI / 2,
    DOWN = Math.PI / 2,
    LEFT = Math.PI,
    RIGHT = 0,
}