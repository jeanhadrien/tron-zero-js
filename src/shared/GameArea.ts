export default class GameArea {
    width: number;
    height: number;

    constructor(width: number = 2000, height: number = 2000) {
        this.width = width;
        this.height = height;
    }
}